# Generic Agent Surface

This folder exists as a compatibility layer for agent ecosystems that expect an `.agents/` path.

It is not the canonical source.

## Skills

`.agents/skills/` should point back to `.ai/skills/`.

That gives generic agent surfaces an easy reference path while keeping the real skill source in one place.
