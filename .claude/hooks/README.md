# Claude Hooks

These hooks are intentionally lightweight.

They exist to:

- surface shared memory on prompt submit
- remind Claude where the memory protocol lives

Keep hook logic small and point back to `.ai/`.
