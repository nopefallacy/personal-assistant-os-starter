description: Save meaningful context at the end of a Claude session.

Read `.ai/playbooks/save-session.md`.

Then:

1. update shared memory only if something durable changed
2. update `.ai/memory/runtime/claude-work-status.md`
3. write or update a shared session log in `.ai/session-logs/`
4. keep the summary short and useful for resuming later

Do not create busywork.
Save only what would matter in the next session.
