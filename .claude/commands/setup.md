description: Personalize this starter into a real executive-assistant system.

Read `README.md`, `CLAUDE.md`, `.claude/README.md`, `.claude/context/CLAUDE.md`, and `.ai/playbooks/setup.md`.

Then read `.ai/skills/assistant-onboarding/SKILL.md` and use it as the setup workflow.

Your job is to personalize this vault into an executive-assistant system for the user's real life and work.

Rules:

- keep the vault lean
- do not add folders just because templates usually have them
- start with `.ai/memory/`
- ask only the highest-signal questions
- keep the shared truth in `.ai/`
- only add Claude-specific behavior if it creates real value

When in doubt, simplify.
