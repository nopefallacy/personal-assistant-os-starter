# TaskNotes

This folder is reserved for task data managed by the TaskNotes plugin.

Use TaskNotes if you want:

- real tasks with dates and priorities
- assistant-managed task capture
- a workflow that is more than a checklist in markdown
