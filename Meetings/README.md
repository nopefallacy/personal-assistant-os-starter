# Meetings

Use this folder only if meetings are part of your real workflow.

Typical subfolders after onboarding:

- `general/`
- `team/`
- `clients/`
- `1-1s/`

Do not create them until you actually need them.
